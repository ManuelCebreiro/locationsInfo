export interface City {
  countryCode: string;
  city: string;
  cityCode: string;
  province: string;
  provinceCode: string;
  community: string;
  communityCode: string;
  latitude: string;
  longitude: string;
}
